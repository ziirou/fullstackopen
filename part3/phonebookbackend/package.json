{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\" && cd ../../part2/phonebook && npm run build && @powershell Copy-Item -Recurse dist ../../part3/phonebookbackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Tero Nikula",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^4.21.2",
    "mongoose": "^8.12.2",
    "morgan": "^1.10.0"
  }
}
